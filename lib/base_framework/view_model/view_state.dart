

enum ViewState {
  idle,
  busy,
  empty,
  error,
  unAuthorized ,
  unBind,
  noNet
}

//enum ConnectivityStatus { WiFi, Cellular, Offline }

//enum ListViewState{idle,busy,empty,error,unAuthorized}















